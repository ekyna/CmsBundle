{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% block title %}
<h1>{{ 'ekyna.page.header.show'|trans({'%name%': page.name})|raw }}</h1>
{% endblock %}

{% block actions %}
{{ ui_link(path('ekyna_page_admin_list'), 'ekyna.admin.button.back_to_list', 'default', 'sm', 'list') }}
{{ ui_link(path('ekyna_page_admin_new'), 'ekyna.page.button.new', 'primary', 'sm', 'plus') }}
{% endblock %}

{% block main %}
<div class="actions">
    {% if cms_content_enabled %}
    {{ ui_link(path('ekyna_page_admin_content', {'pageId': page.id}), 'ekyna.admin.button.content', 'default', 'sm', 'th-large') }}
    {% endif %}
    {{ ui_link(path('ekyna_page_admin_edit', {'pageId': page.id}), 'ekyna.admin.button.edit', 'warning', 'sm', 'pencil') }}
    {{ ui_link(path('ekyna_page_admin_remove', {'pageId': page.id}), 'ekyna.admin.button.remove', 'danger', 'sm', 'trash') }}
</div>
<div class="show show-with-tabs">
    <ul class="nav nav-tabs">
	    <li class="active"><a href="#general" data-toggle="tab">Général</a></li>
	    <li><a href="#seo" data-toggle="tab">Référencement</a></li>
	</ul>
	<div class="show-body">
	    <div class="tab-content">
		    <div class="tab-pane active" id="general">
		        {{ show_row(page.name, 'text', 'Nom') }}
		        <div class="row">
	                <div class="col-md-6">
	                    {{ show_row(page.menu, 'checkbox', 'Menu principal', {'label_nb_col': 4}) }}
	                </div>
	                <div class="col-md-6">
                        {{ show_row(page.footer, 'checkbox', 'Menu pied de page', {'label_nb_col': 4}) }}
	                </div>
	            </div>
	            {% if page.html|length > 0 %}
		        {{ show_row(page.html, 'tinymce', 'Contenu') }}
		        {% endif %}
		    </div>
		    <div class="tab-pane" id="seo">
		        {{ show_row(page.seo.title, 'text', 'Titre') }}
		        {{ show_row(page.seo.description, 'text', 'Description') }}
		        {{ show_row(path(page.route), 'url', 'Url') }}
		        <div class="row">
	                <div class="col-md-6">
	                    {{ show_row(page.seo.changefreq, 'text', 'Fréqu. de modif.', {'label_nb_col': 4}) }}
	                </div>
	                <div class="col-md-6">
                        {{ show_row(page.seo.priority, 'text', 'Priorité', {'label_nb_col': 4}) }}
	                </div>
	            </div>
		    </div>
	    </div>
	</div>
</div>
{% endblock %}

{% block side %}
{% endblock %}