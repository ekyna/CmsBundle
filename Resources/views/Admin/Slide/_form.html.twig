<div class="form-body">
    {{ bootstrap_set_style('horizontal') }}
    {{ bootstrap_set_col_size('md') }}
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.type) }}
    <hr>
    {{ form_rest(form) }}
</div>
